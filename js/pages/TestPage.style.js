import {
    StyleSheet
} from 'react-native';

export var styles = StyleSheet.create({
    container: {
        flex: 1
    },

    field: {
        marginVertical: 10
    },

    textbox: {
        margin: 10
    },

    slider: {
        margin: 10
    }
});